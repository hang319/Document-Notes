# harmonyOs

## 一、装饰器

| 名称 | 功能描述 | 
| ------- | ----- |
| @Entry  |  程序入口    |
| @Component  |  组件    |
| @State  |  数据    |
| build(){}  | 构建    |

## 二、变量定义

```js
 let title: string = "";
 let couont: number = 1;
 let flag: boolean = true;
 let list = string[]: ['', '', ''];
 let obj: GoodsType = {};
 interface GoodsType = { title: string }
 let sum = (num1: number, num2: number) => return num1 + num2;
```

## 三、布局

先布局，再内容。

```js
Column() {
    Row() {
        Text('one')
        Text('two')
        Text('three')
    }
}
```

## 四、属性名

| 名称 | 功能描述 | 
| ------- | ----- |
| width  |  -    |
| height  |  -    |
| backgroundColor  |  -    |
| fontSize  |  -    |
| fontColor  |  -    |
| fontWeight  |  -    |
|padding  |  {left: '', top: ''}   |
|border|{width: 3, color: '', style: BorderStyle.Dashed, radius: 5}|


## 五、图片

`$r('app.xxx')` 代表 `resources/base` 下的图片路径

```js
 Image($r('app.media.background'))
```

## 六、占满系统界面

`expendSafeArea([SafeAreaType.SYSTER, [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]])`



## 七、设置滚动条隐藏

```js
List() {
    ListItem(){}
    ListItem(){}
}
    .scrollBar(BarState.Off)
```

## 八、判断条件

```js
if() {}
else if() {}
else {}
```

## 九、循环

```js
ForEach(names, (item: xxx, index: number) => {
    Text(item)
})
```

## 十、V2 版本状态

```js
@ComponentV2
struct Index {
    @Local num: number = 1;
}

console.log(this.num);
```

## 11、点击事件

```js
组件
.onClick(() => {})
```

## 12、自定义函数组件

```js
@Builder
comp(text: string) {}

this.comp('111');
```

## 13、绝对定位

```js
Row() {
    Stack() {
        Text('111')
    }
}
```

## 14、module.json5

用于修改app 图标、名称等。


#