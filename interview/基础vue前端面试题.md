# 面试1

#### 1、`v-for` 和 `v-if` 可以共用吗

在处于同一节点上，因为 `v-for` 的优先级比 `v-if` 更高，`v-if` 将分别重复运行于每个 `v-for` 循环中

#### 2、在created 和 mounted 这两个生命周期有什么区别

created 是打开页面执行

mounted 是视图加载后才执行。

mounted 在 created 之后。

#### 3、说一下 v-model 的原理，现在需要你写一个 父子组件的 v-model，说一下思路。

父子组件中，实现双向数据绑定的语法糖。

如果没说到子组件的 `model` 可以问下这里面需要写什么？

`props: 'value'` 和 `event: 'change'`。

`value` 还需要在哪里注册一次？`props`

`change` 怎么使用 `$emit('change')`

#### 4、`v-for` 使用 `key` 有什么作用？使用 `key` 需要注意什么？

不增加 key 的时候，数据变化采用的是“就地更新“的策略，更新每个数据。

增加 key 当数据进行插入，移动，删除等，会通过 `diff` 算法识别需要变动的数据位置进行更新，而不是全量更新，更高效的处理 DOM 的更新和保证正确性。

key 需要保持唯一性，否则会出现数据错乱的情况，key 以 `string` 类型为好，不建议放对象作为key，不建议以数组的 index 作为 key

#### 5、多层级组件如何更好的做到数据的传递

1）通过 `provide` 进行数据的注入，通过 `inject` 进行数据获取。

再问一下 `provide` 可以传递 `data` 下的数据吗？可以的，数据会同步更新(相应式更新)。

使用 `provide`, `inject` 可以子组件修改父组件的数据吗？不行。这个方式只适合做数据传递。

2）vuex 和 pinia 进行处理。

#### 6、删除对象用delete和Vue.delete有什么区别？

- delete：只是被删除对象成员变为' '或undefined，其他元素键值不变
- Vue.delete： 直接删除对象成员，保证触发更新视图

#### 7、watch 和 计算属性 `computed` 有什么区别？

- `computed` 用于计算已有数据的计算和更新，数据会被缓存，只有依赖的数据更新才会被重新计算，适合有返回值的场景，
- watch 用于监听某个数据的变化，进而执行对应的操作，适合无返回值的场景。

#### 8、说一下 flex 布局，你使用 flex 的哪些属性，我现在希望在一个正方形的中间放置文字，请直接说 css 代码。这里可以穿叉

- 左对齐
- 右对齐
- 如何自动占满剩余内容
- 当内容过长，如何保证图片或者文字不被压缩
- 如何设置换行与不换行

#### 9、说一下 grid 布局

#### 10、说一下 proxy 代理

- key 的作用是什么：识别作用
- changeOrigin： 控制代理请求时 Host 头的来源，常用于本地开发跨域代理。
- pathRewrite：对别名进行替换

#### 11、如何解决 css 污染

- 命名的规范性
- 通过less 或者 scss 实现样式层级